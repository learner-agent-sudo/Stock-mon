# Copy this file to .env and fill in your values
# .env is gitignored

# Phase 2 (email) — leave blank for Phase 1
EMAIL_TO=
EMAIL_FROM=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Optional: override DB path
# BRIEFING_DB_PATH=data/briefing.db
